# Templates

Jinja2 templates used by the Flask frontend.

Key files
- `index.html` — main UI. It relies on static assets in `static/`.

To change the UI layout, edit `index.html` and refresh the browser.
