# Static

Frontend static assets (JavaScript and CSS) used by the minimal web UI.

Key files
- `app.js` — frontend behavior for the web UI.
- `styles.css` — basic styling.

To modify the UI, edit these files and refresh the browser. No build step required (static files are served by Flask).
